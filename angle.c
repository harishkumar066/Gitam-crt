#include<stdio.h>
int main()
{
    int a,b,c,sum;
    scanf("%d%d%d",&a,&b,&c);
    if(sum==180)
    printf("angles of triangle");
    else
    printf("not angles of triangle");
    return 0;
}
    
    
